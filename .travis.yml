language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
sudo: required
services:
  - docker

before_install:  
  - npm test
  - docker build -t khairul3232/docker-react -f Dockerfile.dev .

script:  
  - docker run -it khairul3232/docker-react npm run test -- --coverage